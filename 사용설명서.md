# Threads 자동 발행 대시보드 사용설명서

이 문서는 프로그램을 처음 받은 사람이 **설정부터 실제 발행까지** 혼자서 따라 할 수 있도록
쓴 사용 설명서입니다. 개발 관련 세부사항(파일 구조 등)은 `README.md`를 참고하세요.

---

## 목차

1. [이 프로그램은 무엇인가요](#1-이-프로그램은-무엇인가요)
2. [시작 전 준비물](#2-시작-전-준비물)
3. [설치하고 처음 실행하기](#3-설치하고-처음-실행하기)
4. [설정 화면 세팅하기 (자세히)](#4-설정-화면-세팅하기-자세히)
5. [상품 등록하기](#5-상품-등록하기)
6. [글쓰기 & 바로 발행하기](#6-글쓰기--바로-발행하기)
7. [AI로 실사용 이미지 만들기](#7-ai로-실사용-이미지-만들기)
8. [예약 발행 (PC를 꺼도 자동으로 올라갑니다)](#8-예약-발행-pc를-꺼도-자동으로-올라갑니다)
9. [발행 후 관리](#9-발행-후-관리)
10. [자주 겪는 문제](#10-자주-겪는-문제)
11. [꼭 알아두어야 할 점](#11-꼭-알아두어야-할-점)

---

## 1. 이 프로그램은 무엇인가요

네이버 스마트스토어 상품을, 실제 구매 후기(또는 상품 정보만)를 바탕으로 사람이 쓴 것 같은
글로 재구성해서 Threads에 자동으로 올려주는 로컬 프로그램입니다.

- **BYOK(Bring Your Own Key)** 방식입니다 — Meta(Threads), Claude, 네이버, (선택)OpenAI,
  (선택)카카오 API를 **본인 명의로 직접 발급**받아 이 프로그램에 등록해서 씁니다.
- 모든 데이터(설정값, 상품, 글, 발행 이력)는 **이 컴퓨터 안의 로컬 파일(`data/app.db`)에만**
  저장됩니다. 외부 서버로 전송되지 않습니다.
- 아래 2가지 모드를 지원합니다.
  - **후기리뷰 모드**: 실제 구매 후기를 인용해서 글을 씁니다. "제가 써보니" 식으로 지어내지
    않고, "구매하신 분들이 이렇게 말씀해주시더라고요" 식으로 실제 후기만 인용/종합합니다.
  - **상품홍보 모드**: 아직 후기가 없는 신상품용. 상품명·카테고리·셀링포인트만으로 글을
    쓰고, 사용 경험을 지어내지 않습니다.

---

## 2. 시작 전 준비물

| 항목 | 필수 여부 | 용도 |
|---|---|---|
| Meta(Threads) 개발자 계정 | **필수** | Threads에 실제로 글을 올리려면 필요 |
| Claude API 키 (Anthropic) | **필수** | 글 자동 생성 |
| 네이버 커머스API 키 | 선택 | 스마트스토어 상품을 자동으로 불러올 때 필요 (안 쓰면 상품을 수동으로 입력) |
| OpenAI API 키 | 선택 | "실사용 사진처럼 보이는" AI 이미지를 만들 때만 필요 |
| 카카오 개발자 앱 | 선택 | 발행될 때마다 카카오톡으로 알림 받고 싶을 때 |
| GitHub 계정 + 저장소 | 선택 (예약 발행용) | PC를 꺼도 정해진 시간에 자동 발행되게 하려면 필요 |
| Python 3.11 이상 | **필수** | 로컬 프로그램 실행 환경 |

전부 다 있어야 시작할 수 있는 건 아닙니다. **Threads + Claude 2개만 있어도** 상품을
수동으로 입력해서 바로 발행하는 것까지는 가능합니다. 나머지는 필요할 때 하나씩 추가하면 됩니다.

---

## 3. 설치하고 처음 실행하기

터미널(명령어 창)을 쓸 필요 없이, **`시작하기.bat` 파일을 더블클릭**하기만 하면 됩니다.

1. 이 컴퓨터에 Python이 설치돼 있지 않다면, `시작하기.bat`이 안내 문구를 띄워줍니다.
   [python.org/downloads](https://www.python.org/downloads/)에서 내려받아 설치하되,
   설치 화면 맨 아래 **"Add python.exe to PATH"** 체크박스를 꼭 체크하세요. 설치가
   끝나면 `시작하기.bat`을 다시 더블클릭합니다.
2. 처음 실행할 때는 검은 창(콘솔)이 뜨면서 필요한 프로그램을 자동으로 설치합니다
   (몇 분 정도 걸릴 수 있습니다). "설치가 끝났습니다!" 문구가 뜨면 완료입니다.
3. 설치가 끝나면(또는 두 번째 실행부터는 바로) 브라우저가 자동으로 열리며
   `http://127.0.0.1:8765` 대시보드가 뜹니다. 처음 실행이면 아무 API도 등록돼 있지
   않아 **설정** 화면으로 안내됩니다.

> 이후로는 이 폴더의 `시작하기.bat`을 더블클릭하기만 하면 계속 다시 쓸 수 있습니다.
> 뜬 검은 창을 닫으면(또는 그 창에서 `Ctrl+C`) 로컬 프로그램은 꺼지지만, **예약 발행은
> 별도로 GitHub Actions가 대신 처리**하므로 영향이 없습니다 (8장 참고). 창은 그대로
> 켜둔 채로 브라우저만 쓰면 됩니다.
>
> (개발자이거나 터미널 사용에 익숙하다면, 기존 방식대로 `python -m venv .venv` →
> `pip install -r requirements.txt` → `python webapp/app.py` 를 직접 실행해도 동일하게
> 동작합니다. `README.md`에 더 자세히 설명돼 있습니다.)

---

## 4. 설정 화면 세팅하기 (자세히)

왼쪽 메뉴 **설정**에서 아래 항목을 순서대로 채웁니다. 각 항목 아래 "연결 테스트" 버튼으로
바로 성공/실패를 확인할 수 있습니다.

### 4.1 Threads (Meta) — 필수

1. [developers.facebook.com](https://developers.facebook.com) 에 로그인 후 **My Apps →
   Create App** 으로 새 앱을 만듭니다.
2. 앱 대시보드에서 **이용 사례 추가(Add use case) → "Access the Threads API"** 를 추가합니다.
3. **Threads API 액세스 → 설정** 화면에서 **App ID / App Secret**을 확인해 아래에 붙여넣습니다.
   > ⚠️ 만약 Redirect URI가 저장이 안 되는 경우, 먼저 **앱 설정 → 기본 설정 → 앱 도메인**에
   > 아무 도메인이나(예: `example.com`) 등록해두고 다시 시도하세요. Meta 콘솔에서 자주
   > 발생하는 알려진 문제입니다.
4. 같은 화면의 **"사용자 토큰 생성기"** 섹션에서 본인 Threads 계정을 테스터로 추가하고,
   Threads 앱/웹에서 초대를 수락한 뒤 토큰을 생성합니다.
   > 앱이 Meta의 App Review를 통과하기 전에는, 테스터로 직접 등록한 계정만 이 앱으로 글을
   > 올릴 수 있습니다. 본인 계정 1개만 쓸 거라면 이걸로 충분합니다.
5. 생성된 **Access Token**과 **User ID**를 설정 화면에 붙여넣고 저장합니다.
   - 이 토큰은 60일짜리입니다. GitHub Actions로 예약 발행까지 쓸 경우, 저장소에
     `refresh_token.yml` 워크플로우가 있으면 자동으로 갱신됩니다.

### 4.2 Claude API — 필수

1. [console.anthropic.com](https://console.anthropic.com) → **API Keys → Create Key**
2. 발급된 키를 설정 화면의 "Claude API" 항목에 붙여넣고 저장합니다.

### 4.3 OpenAI API — 선택 (AI 이미지 생성용)

상품 실사용 사진을 AI로 만들고 싶을 때만 필요합니다 (7장 참고).

1. [platform.openai.com](https://platform.openai.com) 에서 로그인/가입
2. 우측 상단 프로필 → **Billing** → 결제 수단(카드) 등록
   (이미지 생성은 사용량만큼 별도 과금되는 종량제라, 결제 수단이 없으면 호출 자체가 막힙니다)
3. 좌측 메뉴 **API keys → Create new secret key** → 이름은 아무거나 입력 후 생성
4. 뜨는 키(`sk-...`)를 복사해 설정 화면에 붙여넣습니다.
   > 이 창을 닫으면 키를 다시 볼 수 없습니다. 잃어버리면 새로 발급해야 합니다.

### 4.4 네이버 커머스API — 선택 (상품 자동 가져오기용)

1. [apicenter.commerce.naver.com](https://apicenter.commerce.naver.com) 에 로그인 후
   **"내 스토어 어플리케이션"**을 발급받습니다.
   > 반드시 **본인 명의로 직접 발급**받아야 합니다 (위임 인증 방식은 지원하지 않습니다).
   > 후기리뷰 모드만 쓰고 상품을 직접 입력할 거라면 비워둬도 됩니다.
2. 발급받은 **Client ID / Client Secret**을 설정 화면에 입력합니다.
3. **스토어 URL 슬러그**는 "상품 불러오기"를 처음 실행하면 API로 자동 조회되어 채워집니다.
   조회가 실패한 경우에만 `smartstore.naver.com/이 부분` 을 직접 입력하세요.

### 4.5 카카오톡 "나에게 보내기" 알림 — 선택

Threads에 발행될 때마다(성공/실패 모두) 카카오톡으로 알림을 받고 싶을 때 설정합니다.

1. [developers.kakao.com](https://developers.kakao.com) → **애플리케이션 추가** →
   **REST API 키** 확인
2. **제품 설정 → 카카오 로그인** 활성화, **Redirect URI** 등록
   (Threads 설정 때 만든 GitHub Pages `callback.html` 페이지를 그대로 재사용해도 됩니다)
3. **제품 설정 → 카카오 로그인 → 동의항목**에서 **"카카오톡 메시지 전송(talk_message)"**을
   사용 설정합니다.
4. **Client Secret**을 발급받아 설정 화면에 REST API 키/Client Secret/Redirect URI를
   입력하고 저장합니다.
5. 저장 후 화면 아래 **"카카오 인증 시작하기"** 클릭 → 카카오 로그인/동의 → 리다이렉트된
   페이지에 뜬 코드(또는 전체 URL)를 그대로 복사해 아래 입력란에 붙여넣고 **"토큰 발급"**
   클릭.
6. "✅ 토큰이 저장되어 있습니다" 문구가 뜨면 완료입니다. **"발행될 때마다 카카오톡으로
   알림 받기"** 체크박스도 켜주세요.
   > 액세스 토큰은 6시간마다 만료되지만, 발송 직전에 항상 자동으로 갱신되므로 신경 쓸
   > 필요는 없습니다. 다만 리프레시 토큰 자체는 2개월 후 만료되며 자동으로 갱신되지
   > 않으므로, 2개월에 한 번 정도는 5번 과정을 다시 해줘야 합니다.

### 4.6 Instagram — 선택

Threads와 함께 Instagram에도 동시에 발행하고 싶을 때 설정합니다 (발행 미리보기 화면에
"Instagram에도 함께 발행" 체크박스가 추가로 뜹니다).

1. 인스타그램 앱에서 계정을 **프로페셔널 계정**(비즈니스)으로 전환합니다 (프로필 편집 화면
   맨 아래 "프로페셔널 계정으로 전환"). **Facebook 페이지 연결은 필요 없습니다.**
2. [developers.facebook.com](https://developers.facebook.com) → 내 앱 → (Threads용으로
   이미 만든 앱을 그대로 재사용해도 됩니다) → 오른쪽 위 **"이용 사례 추가"** → **"Instagram에서
   메시지 및 콘텐츠 관리"** 추가.
3. 왼쪽 메뉴 **"앱 역할 → 역할"**에서 **"사람 추가"** → 발행에 쓸 그 Instagram 계정을
   **"Instagram 테스터"**로 초대 → **그 Instagram 계정으로 직접 로그인해서 초대를 수락**합니다
   (수락하지 않으면 다음 단계에서 막힙니다).
4. **"이용 사례 → Instagram API → 맞춤 설정"**으로 들어가서:
   - "1. 필수 메시지 권한 추가"에서 **"Add all required permissions"** 클릭
   - 왼쪽 **"권한 및 기능"** 탭에서 `instagram_business_content_publish`(게시 권한)도 찾아서 추가
5. **"2. 액세스 토큰 생성"**에서 **"계정 추가"** 클릭 → 3번에서 초대한 Instagram 계정으로
   로그인/승인 → 계정이 목록에 추가되면 그 옆 **"토큰 생성"** 클릭.
6. 발급된 **토큰**과, 계정명 바로 아래 표시되는 **계정 ID(숫자)**를 복사해서 설정 화면에 입력하고 저장합니다.

> ⚠️ Instagram은 Threads와 달리 **텍스트만으로는 발행할 수 없고 사진이 최소 1장** 필요합니다.
> 발행 미리보기에서 이미지를 선택하지 않으면 "Instagram에도 함께 발행" 체크박스가 비활성화됩니다.
> 또한 캡션 안의 링크는 클릭이 안 되기 때문에, 링크는 본문 대신 **발행 직후 첫 댓글**로 자동으로 달립니다.
>
> 발급된 토큰이 **"IGAA"로 시작**해야 정상입니다. 이 방식(Instagram 로그인이 포함된 API)은
> Facebook 페이지에 연결하는 옛날 방식과는 다른 종류의 토큰을 발급하며, 서로 호환되지 않습니다.

### 4.7 연결 테스트

각 섹션 아래(또는 화면 맨 아래 "연결 테스트" 항목)에 있는 버튼으로 Threads/Claude/네이버/
카카오/Instagram 연동이 실제로 되는지 바로 확인할 수 있습니다. 여기서 실패가 뜨면 위 단계를
다시 점검하세요.

---

## 5. 상품 등록하기

왼쪽 메뉴 **상품 목록** 화면에서 상품을 준비합니다.

- **네이버에서 자동으로 불러오기**: "네이버에서 상품 더 불러오기" 버튼을 누르면 최근 등록
  순으로 50개씩, 이미 불러온 상품은 제외하고 가져옵니다. 등록일도 함께 표시됩니다.
- **수동으로 추가**: 왼쪽 메뉴 **상품 추가**에서 상품명 등을 직접 입력합니다.

상품을 클릭해 상세 화면으로 들어가면:

1. **모드**를 고릅니다 — 후기리뷰 / 상품홍보
2. **링크 위치**를 고릅니다 — 답글로 자동 첨부(권장, 도달률에 유리) / 본문에 직접 포함
3. **스마트스토어 URL / CTA 문구 / 핵심 셀링포인트**를 입력합니다.
   - "AI로 셀링포인트 3개 추천받기" 버튼을 누르면 상품명·카테고리(후기리뷰 모드면 등록된
     후기까지)를 참고해 Claude가 자동으로 채워줍니다.
4. **후기리뷰 모드**라면, 실제 구매 후기를 그대로(또는 개인정보만 제거하고) 옮겨 붙여넣습니다.
   여기 등록된 후기만 글 생성의 근거로 쓰입니다 — 없는 내용을 지어내지 않습니다.
5. **이미지**: "네이버에서 이미지 더 가져오기" 버튼을 누르면 대표/추가 이미지뿐 아니라
   **상세설명 페이지에 들어있는 사진**까지 함께 가져와서, 나중에 미리보기 화면에서 원하는
   사진을 골라 쓸 수 있습니다 (상세페이지에서 가져온 사진은 "상세페이지" 배지로 표시됩니다).

---

## 6. 글쓰기 & 바로 발행하기

상품 상세 화면에서:

1. **"글 생성하기 (바로 발행)"** 클릭 → Claude가 후크가 담긴 글을 자동으로 씁니다.
2. **미리보기** 화면으로 이동합니다.
   - **본문 수정**: 텍스트 박스에서 바로 고쳐 쓴 뒤 "본문 저장"을 누르면 반영됩니다
     (Threads는 500자 제한이 있어 글자 수가 화면에 표시됩니다).
   - **이미지 선택**: 상품에 등록된 사진 중 첨부할 사진을 체크박스로 고릅니다. 1장이면
     단일 이미지, 2장 이상이면 캐러셀(최대 20장)로 올라가고, 0장이면 텍스트만 올라갑니다.
3. 마음에 들면 **"Threads에 발행"** 클릭 — 실제로 계정에 게시됩니다.
   - 마음에 안 들면 "다시 생성"으로 새로 써볼 수 있습니다.
   - 링크를 답글로 설정해뒀다면, 본문 발행 직후 자동으로 답글도 함께 올라갑니다.
   - Instagram을 설정해뒀다면 **"Instagram에도 함께 발행"**도 체크할 수 있습니다 (이미지를
     최소 1장 선택해야 켤 수 있습니다). Threads가 성공한 뒤 Instagram만 실패하더라도 Threads
     발행 자체는 그대로 유지됩니다.
     > Threads는 페르소나 규칙상 해시태그를 쓰지 않지만, Instagram은 탐색/검색 노출에
     > 해시태그가 중요해서 Claude가 상품 정보를 바탕으로 해시태그 8~12개를 추천해 캡션
     > 끝에 자동으로 붙여줍니다 (본문 내용 자체는 Threads와 동일).

발행이 끝나면 카카오 알림(설정했다면)이 오고, 그 상품은 **상품 목록**에서 자동으로 빠지고
**발행된 상품** 페이지로 옮겨집니다.

---

## 7. AI로 실사용 이미지 만들기

상품 사진이 카탈로그 컷뿐이라 "실제 쓰는 모습" 사진이 없을 때 쓰는 기능입니다. 상품 상세
화면의 **"AI 이미지"** 버튼으로 들어갑니다. (OpenAI API 키가 설정돼 있어야 합니다 — 4.3 참고)

1. 화면에 나열된 상품 사진(대표/추가/상세페이지) 중 **기반이 될 실제 사진**을 하나 고릅니다.
2. **"선택한 사진 기반으로 AI 이미지 2장 생성"** 클릭 — AI가 상품의 실제 디자인·색상·로고는
   그대로 유지한 채, 실제 사람이 사용하는 자연스러운 장면으로 편집해서 2장을 만들어줍니다.
   > 텍스트만으로 처음부터 새로 그리는 방식이 아니라, **실제 사진을 편집하는 방식**이라
   > 상품이 실물과 다르게 나오지 않습니다.
3. 마음에 드는 사진이 있으면 아래 **"AI로 수정"** 입력란에 원하는 방향(예: "배경을 실내
   거실로 바꿔줘")을 적고 다시 수정할 수 있습니다. 원본은 그대로 남고 수정본이 새로 추가됩니다.
4. 쓰고 싶은 사진에서 **"이 이미지를 상품 이미지로 사용"**을 누르면 그 상품의 대표 이미지로
   바로 지정되고, 자동으로 **상품 목록**으로 돌아갑니다.

> AI로 만든 이미지는 처음엔 이 컴퓨터에만 저장돼 있지만, 발행(즉시 발행이든 예약 발행이든)
> 시점에 프로그램이 자동으로 GitHub 저장소에 올려 공개 주소로 바꾼 뒤 Threads에 전달하고,
> 발행이 끝나면 그 사본을 다시 정리합니다 — 별도로 신경 쓸 필요 없이 자동으로 처리됩니다.
>
> ⚠️ 재구성한 후기나 AI로 만든 "실사용 사진"을 실제처럼 올릴 때는, 과장·오인 소지가 없도록
> 표현에 유의하세요 (표시·광고의 공정화에 관한 법률 관련).

---

## 8. 예약 발행 (PC를 꺼도 자동으로 올라갑니다)

로컬 프로그램은 켜져 있을 때만 동작하므로, "PC를 꺼둬도 알아서 올라가는" 예약 발행은
**GitHub Actions(클라우드)**가 대신합니다. 글 생성(Claude 호출)은 로컬에서 미리 끝내고
검토까지 마친 다음, 완성된 텍스트만 GitHub에 반영해두면 GitHub Actions가 정해진 시간대
안의 랜덤한 시각에 그대로 발행합니다.

### 8.1 준비물 — GitHub 저장소 + Secrets

**"Secret"**은 GitHub 저장소에 안전하게 보관하는 비밀값 저장소입니다. 로컬 설정 화면(4장)에
입력해둔 토큰/ID를, PC가 꺼져 있어도 GitHub Actions(클라우드)가 대신 쓸 수 있도록 **똑같은
값을 저장소에도 한 번 더 등록**해주는 과정입니다. 즉, "어디서 값을 발급받는지"는 4장을 그대로
따라가면 되고, 아래는 "발급받은 그 값을 GitHub 어디에 붙여넣는지"만 다룹니다.

1. 이 프로젝트 폴더를 본인 GitHub 저장소에 push해둡니다.
2. 브라우저에서 **본인 저장소 페이지 → 위쪽 탭 중 "Settings"** 클릭
   (저장소 최상위 탭이며, 본인 계정의 전체 설정이 아니라 **이 저장소의** Settings여야 합니다).
3. 왼쪽 메뉴에서 **"Secrets and variables"** 클릭 → 펼쳐지는 하위 메뉴에서 **"Actions"** 클릭.
4. **"New repository secret"** 버튼(초록색, 우측 상단) 클릭.
5. **Name**란에 아래 표의 Secret 이름을 **정확히 그대로**(대소문자/언더바 포함) 입력하고,
   **Secret**란에 해당하는 값을 붙여넣은 뒤 **"Add secret"** 클릭.
6. 등록할 값이 여러 개면 4~5번을 값마다 반복합니다. (이미 등록된 이름으로 다시 등록하면
   기존 값을 덮어씁니다 — 토큰을 재발급했을 때 갱신하는 방법이기도 합니다.)

| Secret 이름 | 값을 어디서 받나요 | 필수 여부 |
|---|---|---|
| `THREADS_ACCESS_TOKEN` | 4.1에서 발급받아 로컬 설정 화면에 저장한 Threads Access Token | 필수 |
| `THREADS_USER_ID` | 4.1에서 확인한 Threads User ID | 필수 |
| `KAKAO_CLIENT_ID` / `KAKAO_CLIENT_SECRET` / `KAKAO_REFRESH_TOKEN` | 4.5에서 저장된 값과 동일 (3개 모두 각각 별도 Secret으로 등록) | 선택 (없으면 카카오 알림만 조용히 생략) |
| `INSTAGRAM_ACCESS_TOKEN` | 4.6에서 발급받아 저장한, `IGAA`로 시작하는 토큰 | 선택 (없으면 Instagram 동시예약만 조용히 생략) |
| `INSTAGRAM_BUSINESS_ID` | 4.6에서 확인한 Instagram 계정 ID(숫자) | 선택 (Instagram 예약을 쓸 거면 위 토큰과 함께 필수) |

> 로컬 설정 화면에 입력한 값을 다시 확인하고 싶다면: 액세스 토큰류는 비밀번호 입력칸이라
> 화면에서 그대로 복사할 수 없습니다(보안을 위한 브라우저 기본 동작). 이 경우 값을 다시
> 발급받아 설정 화면에 새로 저장한 뒤, 그 화면에서 바로 복사해 GitHub에 붙여넣는 것이
> 가장 확실합니다. Instagram 계정 ID처럼 일반 텍스트 입력칸은 설정 화면에서 그대로 복사할
> 수 있습니다.
>
> 등록이 잘 됐는지는 Secret 값 자체로는 확인할 수 없지만(GitHub이 값을 다시 보여주지 않습니다),
> **Actions** 탭에서 워크플로우를 한 번 수동 실행(`workflow_dispatch`, "Run workflow" 버튼)해
> 로그에 원인 모를 인증 에러가 없는지로 간접 확인할 수 있습니다.

### 8.2 대기열에 올리고 발행 시간대 정하기

1. 상품 상세 화면에서 **"예약 대기열에 추가"** 클릭 → 글이 초안으로 저장됩니다. Instagram을
   설정해뒀고 그 상품에 기본 이미지가 있으면, 같은 화면에 **"Instagram도 함께 예약"**
   체크박스가 함께 뜹니다 (사진이 없으면 비활성화됨 — Instagram은 사진 없이는 발행 불가).
   옆의 **"발행 날짜"** 입력칸을 클릭하면 달력이 열리고, 특정 날짜를 골라두면 그 날짜가
   될 때까지 기다렸다가 발행됩니다. 비워두면(기본값) 대기열에 넣은 순서대로 다음 활성
   시간대에 자동 발행됩니다. 대기열에 이미 넣은 항목의 날짜도 **예약 대기열** 화면에서
   같은 방식으로 나중에 지정/변경할 수 있습니다.
2. 왼쪽 메뉴 **예약 대기열**에서 생성된 글을 검토합니다. 마음에 안 들면 "다시 생성",
   직접 고치고 싶으면 텍스트 박스에서 수정 후 "텍스트 저장".
3. 마음에 들면 **"승인"** 클릭 (상태가 READY로 바뀝니다).
4. 화면 위쪽에서 **발행 시간대**를 켭니다 — 아침(07~09시) / 점심(12~13시) / 오후(15~17시) /
   저녁(19~22시, 참여도가 가장 높음). **켠 시간대 개수 = 하루 최대 발행 건수**(최대 4건)입니다.
   하루 4개 모두 Instagram에도 동시예약하고 싶다면 4시간대를 모두 켜고, 각 상품을 대기열에
   추가할 때마다 "Instagram도 함께 예약"을 체크하면 됩니다.

### 8.3 GitHub에 반영하기

**"승인된 항목 GitHub에 반영"** 버튼을 누르면 승인된(READY) 글들이 `data/queue.json`으로
저장돼 자동으로 커밋·푸시됩니다. (AI 이미지를 쓴 항목이 있으면 그 사진도 함께 GitHub에
올라가 Threads가 읽을 수 있는 공개 주소로 자동 변환됩니다.) Instagram을 함께 예약한
항목은 이 시점에 Claude가 해시태그 8~12개를 추천해 캡션을 미리 완성해둡니다(발행 미리보기와
동일한 방식). 이후 GitHub Actions는 Threads 발행이 성공한 뒤 이어서 이 캡션으로 Instagram에도
발행하고, 실패하더라도 이미 성공한 Threads 발행에는 영향을 주지 않습니다.

이후 GitHub Actions(`scheduled_publish.yml`)가 15분마다 깨어나 "지금이 켜둔 시간대의
목표 시각을 지났는지"를 확인하다가, 지났으면 대기열에서 가장 오래된 글 1건을 그대로
발행합니다. **이 단계는 Claude를 호출하지 않고, 로컬에서 승인한 텍스트를 그대로 씁니다.**
발행이 끝나면 관련 이미지 사본도 자동으로 정리됩니다.

### 8.4 발행 결과 확인하기

나중에 로컬 프로그램을 다시 켰을 때, **예약 대기열** 화면에서 **"GitHub에서 발행 결과
가져오기"**를 누르면 실제로 발행된 내용이 로컬 **발행 이력**에 반영됩니다.

> GitHub Actions 실행 내역은 저장소의 **Actions** 탭에서 직접 확인할 수도 있습니다.
> 워크플로우 이름은 "Threads 예약 발행 (랜덤 시간대)"입니다.

---

## 9. 발행 후 관리

- **상품 목록**: 아직 발행 안 한 상품만 보여줍니다. 대기열에 승인되지 않은 초안이나
  승인된(READY) 글이 있는 상품에는 **"발행대기"** 배지가 표시됩니다.
- **발행된 상품**: 한 번이라도 발행된 상품만 모아서, 최근 발행일 순으로 보여줍니다.
- **발행 이력**: 상품별로 지금까지 올라간 글을 모두 볼 수 있고, 마음에 드는 글에 **⭐
  즐겨찾기**를 눌러두면 이후 글 생성(즉시 발행/대기열 추가/재생성 모두)에서 그 글들의
  문체를 예시로 참고합니다. 즐겨찾기를 하나도 안 해두면 문체 학습 없이 기본 페르소나로만
  씁니다.

---

## 10. 자주 겪는 문제

| 증상 | 원인 / 해결 |
|---|---|
| Threads 발행이 "media not found" 등으로 실패 | 일시적인 문제일 수 있습니다. 다시 시도해보세요. |
| "Param image_url is not a valid URI" | AI로 만든 이미지를 발행에 쓰는데, GitHub 공개 업로드 단계가 실패했을 가능성. 인터넷 연결과 GitHub 저장소 push 권한(로컬 git 인증)을 확인하세요. |
| 카카오 알림이 갑자기 안 옴 | 리프레시 토큰이 2개월 지나 만료됐을 수 있습니다. 설정 화면에서 카카오 인증을 다시 진행하세요(4.5). |
| GitHub Actions에서 예약 발행이 계속 스킵됨 | 켠 시간대가 없거나(8.2), 그 시간대에 이미 오늘 발행을 했거나, 아직 목표 시각 전입니다. Actions 탭의 실행 로그에서 `[skip]` 메시지로 정확한 이유를 확인할 수 있습니다. |
| 네이버 상품이 안 불러와짐 | Client ID/Secret이 본인 명의로 직접 발급한 게 맞는지, 설정 화면 "네이버 테스트" 버튼으로 확인하세요. |
| AI 이미지 생성이 실패함 | OpenAI 계정에 결제 수단이 등록돼 있는지 확인하세요 (4.3). |

---

## 11. 꼭 알아두어야 할 점

- **본인 API 키를 직접 발급해서 쓰는 구조(BYOK)**입니다. 다른 사람의 계정과 공유되지
  않고, 이 프로그램을 통해 중앙 서버로 전송되는 데이터도 없습니다.
- 팔로우/좋아요를 자동으로 누르거나, 여러 게시물에 같은 댓글을 반복해서 다는 기능은
  **지원하지 않습니다.** 공식 Threads API에 그런 기능 자체가 없고, 시도할 경우 Meta의
  정책 위반으로 계정이 정지될 수 있습니다.
- 재구성한 후기나 AI로 만든 "실사용 사진"을 쓸 때는, 실제와 다르게 오인될 만한 과장된
  표현은 피하는 것이 좋습니다.
